[project]
name = "usda-gnatsgo-data"
version = "0.1.0"
description = "End-to-end pipeline: USDA NRCS gNATSGO soil database -> Icechunk Zarr store on Source Cooperative"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.13"
dependencies = [
    "icechunk>=2.1",
    "zarr>=3.0",
    "xarray>=2026.7.0",
    "rioxarray>=0.22.0",
    "rasterio>=1.5.0",
    "numpy>=2.0",
    "pyarrow>=25.0.0",
    "polars>=1.43.0",
    "duckdb>=1.5.5",
    "pyproj>=3.7",
    "pydantic>=2.10",
    "typer>=0.15",
    "boto3>=1.43",
    "tqdm>=4.69.0",
]

[project.scripts]
usda-gnatsgo = "usda_gnatsgo.cli:app"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-xdist>=3.6",
    "ruff>=0.8",
    "jupyter>=1.1.1",
    "matplotlib>=3.11.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/usda_gnatsgo"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # import sorting
    "UP",  # pyupgrade
    "B",   # bugbear
    "SIM", # simplify
    "RUF", # ruff-specific
]
